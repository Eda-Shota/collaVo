  <% if JoinProject.find_by(user_id: current_user.id, project_id: project.id) %>
    <p class="btm btn- btn-secondary">申し込み済み</p>
  <% else %>
    <%= link_to "参加申し込みをする",  project_join_projects_path(project.id), class: "btn btn-success" %>
  <% end %>